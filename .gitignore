# dependencies
node_modules/
.pnp
.pnp.js

# testing
coverage/

# next.js
.next/
out/

# production
build/

# misc
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (NEVER commit secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# firebase
.firebase/
.firebaserc

# service accounts and secrets
*.secret.json
service-account-*.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
Thumbs.db

# Playwright
/tests/e2e/.auth/
/tests/e2e/.pb-data/
/playwright-report/
/playwright-report-pocketbase/
/test-results/
/.features-gen/
/.features-gen-pocketbase/

# PocketBase binary (root-level only, so src/lib/services/pocketbase/ is not affected)
/pocketbase.exe
/pocketbase
.pb-data/

# Claude Code
.claude/

*-debug.log
typescript

# Basemap tile archives (Phase 8 / §8.B). These are generated by
# scripts/fetch-basemap.sh from the Protomaps daily build, not authored here,
# and are megabytes of binary. The app degrades to the raster-only map when
# they are absent, so a fresh clone works without them.
public/basemap/
.cache/
