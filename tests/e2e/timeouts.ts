/** goto() and waitForURL() — allows page hydration + Firebase Auth emulator to settle. */
export const NAV_TIMEOUT = 15_000;

/** Firebase Storage upload + Firestore write round-trip. */
export const UPLOAD_TIMEOUT = 20_000;
